#ifndef TASK_H
#define TASK_H

#include "array.h"

#include <vector>
#include <algorithm>
#include <ranges>

void find_intersection(Array* array1, Array* array2);
void find_union(Array* array1, Array* array2);

#endif